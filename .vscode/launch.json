{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug current C++ file (LLDB-DAP)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "stopOnEntry": false,
            "preLaunchTask": "clang++: build current file"
        }
    ]
}
